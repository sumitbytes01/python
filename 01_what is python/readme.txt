high level
portable
readable
productive
standard library
multi-use
general purpose
designed for readability, using identation, instead of braces (The use of indentation in 
    Python instead of braces makes the code visually cleaner and enforces a standard style, 
    which can improve readability and reduce errors related to missing or mismatched braces. )
supports multiple programing paradigms: procedural, object orients, functional
simple syntax
python code is executed by interpreter, immidiate execution without any compilation

dynamically typed - variables type are determined at runtime
supports multiple inheritance
concise and readable
The Global Interpreter Lock (GIL) in CPython limits true parallel threads for CPU-bound tasks